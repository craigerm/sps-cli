{%- comment  -%}
  # Auto-generated by build tool. Do not edit!

  Usage:
    render 'app-css', name: 'theme', type: 'bundle' | 'component',  path_only: false, with_url: false, preload: true
{% endcomment -%}
{%- liquid
  assign css_name = ''
  assign warning = false
  assign path_only = path_only | default: false
  assign as_url = as_url | default: false
  assign type = type | default: 'bundle'

  if type == 'bundle'
    assign css_name = '%PREFIX%bundle.' | append: name
  elsif type == 'component'
    assign css_name = '%PREFIX%component-' | append: name
  else
    assign css_name = name
    assign warning = true
  endif

  assign css_name = css_name | append: '%EXT%'

  if path_only and with_url
    echo css_name | asset_url
    break
  elsif path_only
    echo css_name
    break
  endif
-%}
{{ css_name | asset_url | stylesheet_tag: preload: preload }}
{%- if warning -%}
  <script>
    console.error('[app-css] Invalid type specified for: name={{ name }}, type={{ type }}')
  </script>
{%- endif -%}
